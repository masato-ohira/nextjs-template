'use client'

import { Button } from '@ui/button'

const Page = () => {
  return (
    <div>
      <div
        className={`
          container
          mx-auto
          min-h-screen
          center
        `}
      >
        <Button>OK</Button>
      </div>
    </div>
  )
}

export default Page
